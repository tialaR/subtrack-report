// import { generateSubMapId } from "@utils/subMapHelper";
// import { SubMap } from "@components/SubMap";
// import type { Point } from "@components/SubMap/types";

// export const createSubMapComponent = (path: string, imageSrc: string, markers: Point[], uploade: boolean) => {
//   const id = generateSubMapId(path);

//   const NewSubMapComponent: React.FC = () => (
//     <SubMap id={id} image={imageSrc} title={path} markers={markers} uploaded={uploade} />
//   );

//   return NewSubMapComponent;
// };
